<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-end">
        <a class="bg-red-500 px-3 py-2 rounded-md" href="/logout"> Logout</a>
      </div>
      <h5>Edit your post.</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          class="block p-3 w-1/3 h-44 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md mt-5"
          name="content"
          id=""
          cols="30"
          rows="10"
        >
<%= post.content %></textarea
        >
        <input
          class="block px-3 w-40 py-2 bg-yellow-500 rounded-md mt-3"
          type="submit"
          value="Update your Post"
        />
      </form>
    </div>
  </body>
</html>
